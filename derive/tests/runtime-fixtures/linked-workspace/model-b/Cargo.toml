[package]
name = "model-b"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
qubit-model-derive = { path = "../../../../" }
qubit-model-metadata = { path = "../../../../../" }
serde = { version = "1", features = ["derive"] }

[features]
duplicate-fixture = []
