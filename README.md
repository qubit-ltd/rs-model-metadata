# Qubit Model Metadata

Static, strongly typed metadata for Rust domain models.

The model metadata API is specified in [`doc/model-metadata-and-derive-design.md`](doc/model-metadata-and-derive-design.md).

## Status

This crate is initialized and contains no runtime model metadata implementation yet.
